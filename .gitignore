/node_modules
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pids
*.pid
node_modules
# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Optional npm cache
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# dotenv environment variable files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# coverage directory used by tools like istanbul
coverage

# nyc report file
.nyc_output

# mocha test output
mocha_trace

# Grunt intermediate storage (https://gruntjs.com/using-the-cli)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# Go cache and build directory (https://golang.org/pkg/cmd/go/internal/cache/)
# The directory for the cache used by the go build system;
GOMODCACHE=$XDG_CACHE_HOME/go-build
GOCACHE=$XDG_CACHE_HOME/go-build
GOCACHE=~/Library/Caches/go-build
# The build directory for the go command.
GOTMPDIR=$XDG_DATA_HOME/go
GOTMPDIR=~/Library/Developer/Xcode/DerivedData/go-build

# Cache directories
~/.cache

# Optional global cache directory
$HOME/.cache

# Optional global config directory
$HOME/.config

# Optional global data directory
$HOME/.local/share

# Optional global state directory
$HOME/.local/state

# Optional global temporary directory
$HOME/.local/tmp

# Optional global log directory
$HOME/.cache/logs

# Optional global npm cache directory
$HOME/.npm/_cacache

# Optional global yarn cache directory
$HOME/.cache/yarn/v6

# Optional global pnpm cache directory
$HOME/.local/share/pnpm/store/v3

# Optional global pnpm global store directory
$HOME/.local/share/pnpm/global-store/v3

# Optional global pnpm store directory
$HOME/.local/share/pnpm/store/v3

# Optional local cache directory
$XDG_CACHE_HOME/go-build

# Optional local config directory
$XDG_CONFIG_HOME/go

# Optional local data directory
$XDG_DATA_HOME/go

# Optional local state directory
$XDG_STATE_HOME/go

# Optional local temporary directory
$XDG_RUNTIME_DIR/go

# Optional local log directory
$XDG_DATA_HOME/go/logs

# Optional local npm cache directory
$XDG_DATA_HOME/npm/_cacache

# Optional local yarn cache directory
$XDG_CACHE_HOME/yarn/v6

# Optional local pnpm cache directory
$XDG_DATA_HOME/pnpm/store/v3

# Optional local pnpm global store directory
$XDG_DATA_HOME/pnpm/global-store/v3

# Optional local pnpm store directory
$XDG_DATA_HOME/pnpm/store/v3
